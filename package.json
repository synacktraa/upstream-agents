{
  "name": "upstream-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "cd packages/web && next dev --webpack",
    "build": "npm run build -w @upstream/agents && npm run build -w @upstream/web",
    "build:sdk": "npm run build -w @upstream/agents",
    "build:web": "npm run build -w @upstream/web",
    "start": "cd packages/web && next start",
    "lint": "npm run lint --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "5.7.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
