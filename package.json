{
  "name": "upstream-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "postinstall": "npm run build:eliza -w background-agents && cd packages/web && npx prisma generate",
    "dev": "npm run dev -w @upstream/web",
    "dev:legacy": "cd packages/legacy && prisma generate && next dev --webpack",
    "prisma:migrate": "cd packages/web && npx prisma migrate dev",
    "prisma:migrate:deploy": "cd packages/web && npx prisma migrate deploy",
    "prisma:generate": "cd packages/web && npx prisma generate",
    "prisma:status": "cd packages/web && npx prisma migrate status",
    "build": "npm run build -w background-agents && npm run build -w @upstream/legacy && npm run build -w @upstream/web",
    "build:sdk": "npm run build -w background-agents",
    "publish:sdk": "npm publish -w background-agents",
    "build:legacy": "npm run build -w background-agents && npm run build -w @upstream/legacy",
    "build:web": "npm run build -w background-agents && cd packages/web && npx prisma generate && npm run build",
    "start": "npm run start -w @upstream/web",
    "start:legacy": "cd packages/legacy && next start",
    "lint": "npm run lint --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "5.7.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@types/ssh2": "^1.15.5",
    "ssh2": "^1.17.0"
  }
}
